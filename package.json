{
  "scripts": {
    "start": "cd website && yarn && yarn start",
    "pull:pdf-lib": "git remote add pdf-lib git@github.com:Hopding/pdf-lib.git; git fetch pdf-lib Rewrite && git subtree pull --prefix=pdf-lib pdf-lib Rewrite --squash",
    "docs": "rm -rf ./docs/api && cd pdf-lib && ../node_modules/typedoc/bin/typedoc --options ../typedoc.json"
  },
  "devDependencies": {
    "docusaurus-init": "^1.11.1",
    "typedoc": "^0.14.2",
    "typedoc-plugin-markdown": "^2.0.10"
  }
}
